# С1 · Внешний базис блока «Расписание»

Протокол: [RESEARCH-PROTOCOL.md](../../docs/method/RESEARCH-PROTOCOL.md).
Базис версии 1.1.1 ([`../school-onboarding/10-basis.md`](../school-onboarding/10-basis.md),
строки #1…#11) действует целиком и здесь не переписывается. Ниже — только
строки, которых блоку не хватало: они относятся к качеству, корректировке и
выдаче артефактов наружу.

| # | Источник | Требование | Класс | Влияние на конструкцию |
|---|---|---|---|---|
| 12 | Even, Itai, Shamir, «On the complexity of timetable and multicommodity flow problems» (1976) | Задача построения расписания «класс × педагог» с ограничением на кратность принадлежит классу задач, для которых алгоритм полиномиальной сложности не известен | де-факто | Обосновывает три решения разом: названный бюджет перебора (AR-107), локальный поиск вместо второго захода перебора (AR-121) и отказ от обещания «наилучшая сетка» — движок обещает локальный минимум, а не глобальный |
| 13 | МР 2.4.0331-23 (Роспотребнадзор), ранжирование предметов по трудности | Трудные предметы — в часы высокой работоспособности | де-факто | Маркер `prio` получает старший вес (§3.2 спеки). Шкала трудности остаётся слотом: в версии приоритет задаёт человек |
| 14 | СанПиН 1.2.3685-21, п. про сдвоенные уроки в начальной школе | Сдвоенные уроки в начальных классах не проводятся | закон | Маркер `subjectSpread` штрафует два часа предмета в один день; для 1-й параллели это правовой, а не эстетический признак |
| 15 | RFC 5545 (iCalendar) | `VEVENT` с `RRULE` недельного повтора и `EXDATE` для исключённых дат | де-факто стандарт | Календарный экспорт выражает шаблон недели одним правилом на слот, а не списком уроков: список ломается при сдвиге четверти, правило — нет. `EXDATE` заполняется нерабочими днями из календаря (AR-100) |
| 16 | RFC 2104 (HMAC), практика подписанных ссылок | Подпись полезной нагрузки секретом, проверяемая без обращения к состоянию | де-факто стандарт | Ссылка на снимок проверяется без записи в БД; версия сетки входит в подпись, поэтому новая версия обрывает старую ссылку без отдельной операции |
| 17 | ISO/IEC 9241-171, WCAG 2.1 (1.4.1 «Использование цвета») | Цвет не является единственным способом передать информацию | де-факто | Правило маркировки §8: каждый маркер несёт иконку и текст; сетка в оттенках серого читается без потери смысла |
| 18 | Практика ЭЖД (Дневник.ру, ЭлЖур, МЭШ) | Расписание класса публикуется на сайте школы и раздаётся родителям ссылкой | де-факто | Подтверждает контур §9; из практики же взято, что публикуемая сетка не содержит данных учеников — только классы, предметы и педагоги |

## Не найдено

Норм, задающих **меру качества** расписания (что считать лучшим при равной
допустимости), на федеральном уровне нет. СанПиН и МР нормируют границы, а не
предпочтения: «трудные предметы в часы работоспособности» — методическая
рекомендация без числовой шкалы. Поэтому веса маркеров §3.2 спеки помечены
`[дефолт]` владельца и не маскируются под норму: ссылаться на СанПиН в тексте,
объясняющем вес `w.teacherGap`, запрещено.

Требования к сроку жизни публичной ссылки на расписание в источниках также
отсутствуют — 90 дней взяты решением владельца (вопрос В1 спеки).

## Противоречия источников

Строка #14 (сдвоенных уроков в начальной школе нет) и практика профильных
классов старшей школы, где сдвоенные уроки — норма, не противоречат друг другу
в границах версии: маркер `subjectSpread` мягкий, и старшая параллель платит за
сдвоенный урок штрафом, а не отказом. Жёстким это ограничение стало бы только
вместе с параллельно-зависимой политикой сдвоенных уроков — она вне объёма.
