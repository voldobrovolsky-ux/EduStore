# Генеральное описание Schoolium 1.1.1 — ориентир для сверки

> **Назначение.** Один документ, по которому агент проверяет себя: что за
> продукт, из чего он состоит, что обязано существовать к концу работы и чего в
> нём быть не должно. Это **не источник требований** — при расхождении
> побеждают `30-spec.md` (поведение), `70-screens.md` (экраны),
> `75-adaptive.md` (раскладки), `docs/ar/` (решения). Ориентир нужен, чтобы не
> потерять целое, разглядывая частное.
>
> Версия: **1.1.1**. Ветка-источник: `edustore-final-edition`.

---

## 1. Что мы строим, в трёх абзацах

**Schoolium** — ERP для школы: единственный существующий продукт экосистемы
**EduStore**, которая в будущем станет маркетплейсом образовательных сервисов.
Сейчас маркетплейса нет, есть сервис; Schoolium должен быть готов встать в него
позже, но витрина, каталог и биллинг EduStore сейчас не проектируются.

Версия 1.1.1 закрывает один сценарий целиком: **пустая школа превращается в
работающую**. Модератор школы заводит классы и учеников, создаёт предметы,
активирует сотрудников по QR, привязывает педагогов к предметам, задаёт четверти
и нагрузку, запускает автогенерацию расписания и подтверждает её. После этого
журнал получает колонки из расписания, и педагоги начинают ставить отметки.

Пользователи 1.1.1 — **только сотрудники школы**, шесть ролей. Аккаунтов детей и
родителей нет, детских персональных данных как учётных записей нет: ученик
существует записью контингента. Это сознательное снятие всего 152-ФЗ-контура с
первой версии; он вернётся в 1.1.3 вместе с кабинетами детей и родителей.

## 2. Карта документов: что где искать

| Вопрос | Документ |
|---|---|
| Что за продукт, где границы | `docs/PRODUCT-LOGIC.md` |
| Почему принято именно так | `docs/ar/` — 8 блочных реестров, AR-1…AR-87 |
| Как это должно работать | `specs/school-onboarding/30-spec.md` |
| Какие экраны и элементы | `specs/school-onboarding/70-screens.md` |
| Как это в двух раскладках | `specs/school-onboarding/75-adaptive.md` |
| Как это выглядит | `specs/school-onboarding/60-design.md` + `docs/design/tokens.json` |
| Что уже проверено и чем | `specs/school-onboarding/40-bench.md`, `docs/G-CHECKS.md` |
| Эталон поведения генератора | `specs/school-onboarding/model/props.mjs` |
| Что делать по шагам | `specs/school-onboarding/80-code-prompt.md` |
| Что нашёл и починил аудит перед кодом | `specs/school-onboarding/81-audit-stage0.md` |

**Правило приоритета при конфликте:** решение в `docs/ar/` > поведение в
`30-spec.md` > экраны в `70-screens.md` > раскладки в `75-adaptive.md` >
внешний вид в `60-design.md`. Нашёл конфликт — не выбирай молча: почини
документ низшего приоритета и отметь правку.

## 3. Полный инвентарь: что обязано существовать

### 3.1 Роли — 6
- [ ] `founder` учредитель · `director` директор · `deputy_academic` зам по учебной работе
- [ ] `deputy_upbringing` зам по воспитательной работе · `teacher` преподаватель · `moderator` модератор школы
- [ ] Роли **совместимы**: у пользователя массив ролей (AR-60)
- [ ] **Модератор — полные права школы** (AR-88): проходит все 25 мутаций, ставит и снимает отметки в любом уроке, удаляет пользователей; гейты реальности (`LESSON_NOT_HELD`) действуют и на него; каждое его действие — в аудите (G-41)
- [x] Совмещение и разделение обязанностей (AR-61): **предмета в 1.1.1 нет** — операции утверждения чужого артефакта в версии не существует, подтверждает сетку модератор, а предлагает её генератор (AR-18). Ворота G-27 заводятся вместе с первой такой операцией (КТП), а не искусственным сценарием здесь

### 3.2 Экраны — 21
`S-01` вход · `S-02` код · `S-03` регистрация по QR · `S-04` фото ·
`S-10` классы · `S-11` мастер классов · `S-12` карточка класса · `S-13` профиль ученика ·
`S-20` предметы · `S-21` карточка предмета · `S-22` QR привязки ·
`S-30` персонал · `S-31` карточка сотрудника ·
`S-40` расписание · `S-41` настройка · `S-42` предпросмотр ·
`S-50` журнал · `S-51` тема урока · `S-52` выбор отметки ·
`S-60` кабинет модератора · `S-70` сканер

- [ ] У каждого: `loading`, `empty`, `error` — три состояния, не одно
- [ ] У каждого: десктопная и мобильная раскладка по `75-adaptive.md` §6

### 3.3 Модалки — 15
`M-01` мастер классов · `M-02` профиль ученика · `M-03` создание предмета ·
`M-04` карточка предмета · `M-05` QR привязки · `M-06` карточка сотрудника ·
`M-07` добавление роли · `M-08` настройка расписания · `M-09` прогресс генерации ·
`M-10` отказ генератора · `M-11` тема урока · `M-12` выбор отметки ·
`M-13` подтверждение удаления · `M-14` выход из мастера · `M-15` меню пользователя

- [ ] Вложенность не глубже двух уровней
- [ ] `Esc`, крестик, клик по фону; при введённых данных — `M-14`
- [ ] Ловушка фокуса, возврат фокуса при закрытии

### 3.4 Оболочка — 2 варианта
- [ ] Десктоп: сайдбар 240px (сворачиваемый до 72), топбар 64px
- [ ] Мобайл: хедер 56px, таб-бар 64px + safe-area, **пять вкладок для всех ролей**
- [ ] Экраны входа — без оболочки вовсе

### 3.5 Кнопки — 9 типов
`B-primary` · `B-accent` · `B-secondary` · `B-ghost` · `B-danger` · `B-off` ·
`B-icon` · `B-fab` · `B-chip`

- [ ] Одна `B-accent` на экран
- [ ] Недоступная роли кнопка **не рендерится**; `disabled` означает «нельзя сейчас»
- [ ] Мобайл: во всю ширину, высота 48, кроме иконок и чипов

### 3.6 Модели данных — 11
`SchoolClass` · `Student` · `StudentGroup` · `Subject` · `TeacherBinding` ·
`Term` · `ScheduleTemplate` · `TemplateSlot` · `Lesson` · `Mark` · `LessonTopic`

- [ ] `workspaceId` на **каждой** — без исключений (AR-2, AR-44)
- [ ] `Lesson.detachedAt` — урок, выпавший из шаблона при регенерации, но
      сохранивший отметки (AR-85); без этого поля история переписывается
      пересборкой расписания

### 3.7 События — 11
`contingent.class.created.v1` · `contingent.student.upserted.v1` ·
`contingent.student.deactivated.v1` · `staff.member.registered.v1` ·
`subject.teacher.bound.v1` · `calendar.term.set.v1` ·
`schedule.template.confirmed.v1` · `schedule.lesson.materialized.v1` ·
`schedule.lesson.detached.v1` · `journal.mark.posted.v1` · `journal.topic.set.v1`

- [ ] Через outbox, каждый подписчик через inbox (AR-5, AR-24)
- [ ] Журнал строит колонки **подпиской** на материализацию, не чтением таблиц расписания
- [ ] Об исчезновении урока журнал узнаёт `schedule.lesson.detached.v1`, а не сверкой таблиц (AR-85)

### 3.8 Права — 13
Мутационные (8): `school.manage` · `contingent.write` · `subject.write` ·
`staff.manage` · `schedule.build` · `journal.mark.post` · `journal.topic.set` ·
`staff.self.write`.
Читающие (5), выдаются всем шести ролям: `classes.read` · `subjects.read` ·
`staff.read` · `schedule.read` · `journal.read`. Записи «`*.read`» в каталоге
прав не существует — это сокращение текста, а не код.

- [ ] Каждая мутация несёт `@RequirePermission`, каталог пополняется тем же коммитом (AR-35)
- [ ] Все 25 мутаций версии — по таблице `70-screens.md` §11; действия вне таблицы не существует
- [ ] Модератор проходит каждую из 25; четыре читающие роли — ни одной; педагог — только в своих уроках (AR-88, ворота G-41)

### 3.9 Коды ошибок — 19
`CLASSES_ALREADY_EXIST` · `SMS_GATEWAY_DOWN` · `OTP_INVALID` · `OTP_EXPIRED` · `OTP_ATTEMPTS_EXCEEDED` ·
`TOKEN_USED` · `TOKEN_EXPIRED` · `PHONE_TAKEN_IN_SCHOOL` · `TERM_OVERLAP` ·
`TERM_REVERSED` · `LOAD_EXCEEDS_SANPIN` · `LOAD_EXCEEDS_GRID` ·
`GROUP_HOURS_UNEQUAL` · `TEACHER_OVERBOOKED` · `SUBJECT_UNCOVERED` ·
`GROUPS_UNASSIGNED` · `NO_SOLUTION` · `LESSON_NOT_HELD` · `STUDENT_INACTIVE`

- [ ] Тексты — дословно из `70-screens.md` §9, с объектом и цифрами
- [ ] Шесть арифметических отказов считаются **до** перебора, `NO_SOLUTION` — единственный отказ перебора

### 3.10 Отметки — 6 значений
`5` `4` `3` `2` — числовые, в средний балл · `н` отсутствие · `б` болен — не в средний балл (AR-79)

### 3.11 Ворота
- [ ] Существующие не сломаны: G-1 (тенант), G-10 (RBAC-полнота)
- [ ] Написаны и зелёные: G-24 (телефонный контур), G-33 (онбординг), G-34 (генератор), G-35 (журнал), G-36 (QR), G-40 (регенерация не теряет историю), G-41 (полнота прав модератора и аудит)
- [ ] Документные зелёные: G-19, G-20, G-21, G-37, G-38, G-39 — `npm run method:lint`

## 4. Сквозной сценарий приёмки

Версия считается работающей, когда этот путь проходится целиком без обращения к
разработчику — в **обеих** раскладках:

1. Модератор входит по телефону и коду → `S-01`, `S-02`.
2. Создаёт 8 классов без литер, по 15 учеников, 2 группы, 8 мальчиков → `S-11`;
   на шаге литер нажимает «⌀ Без литер», на шаге пола видит «Девочек: 7»,
   в превью читает перечень «1, 2, 3 … 8».
3. Заполняет профили учеников → `S-12`, `S-13`; список пересортировывается,
   строки окрашены по полу, «Ёлкина» встаёт после «Егорова».
4. Создаёт предметы на пары «предмет × класс» → `S-20`.
5. Активирует сотрудников: открывает карточку, показывает QR, сотрудник сканирует,
   вводит ФИО и телефон, пропускает фото → `S-31`, `S-03`, `S-04`.
6. Привязывает педагогов: «+» на карточке предмета, педагог сканирует из
   `S-70`, модератор выбирает «весь класс» либо группы → `S-21`, `S-22`.
7. Задаёт четыре четверти → `S-41` шаг 1; даты уходят в календарь.
8. Вписывает часы каждому педагогу → шаг 2; при перегрузе видит
   `LOAD_EXCEEDS_SANPIN` с именем класса и цифрами.
9. Отмечает приоритетные предметы либо жмёт «⌀ Без приоритетов» → шаг 3.
10. Задаёт параметры дня в границах СанПиН → шаг 4.
11. Запускает генерацию, видит предпросмотр, **нажимает «Подтвердить»** → `S-42`.
12. Журнал показывает колонки по материализованным урокам; 4 сентября с двумя
    уроками даёт две колонки под одним числом → `S-50`.
13. Педагог входит, открывает свой урок, задаёт тему, ставит отметки `5`, `н`,
    `б`; попытка поставить отметку в завтрашний урок отклоняется
    `LESSON_NOT_HELD` → `S-51`, `S-52`.
14. Модератор деактивирует ученика с отметками — история сохраняется, из новых
    колонок ученик исчезает.
15. Модератор добавляет ученика в середине четверти — плашка «расписание
    устарело» **не появляется**; затем меняет нагрузку, регенерирует и
    подтверждает — уроки с отметками остаются колонками с пометкой «вне
    расписания», отметки на месте (`S-50.col.detached`, AR-85).

**Ориентир по нагрузке:** полный онбординг школы 8×15 — около 65 минут работы
одного человека, из них ~40 минут ввод контингента (`model/sim.mjs`). Если
получается заметно дольше — где-то лишние касания.

## 5. Красные линии

Нарушение любой из них — не «спорное решение», а дефект:

1. **Система не решает за человека.** Расписание материализуется только после
   нажатия «Подтвердить». Автоприменение по таймеру или «раз всё зелёное» — запрещено (AR-18).
2. **Изоляция школ не обсуждается.** `workspaceId` на каждой таблице; общей
   таблицы поверх школ не существует ни для какого удобства (AR-2, AR-44).
3. **Гейт живёт в контракте, а не в интерфейсе.** Серые будущие колонки — это
   отражение проверки, а не сама проверка (AR-74).
4. **Смысл не кодируется одним цветом.** У отметок есть символ, у пола — позиция
   строки; человек с дальтонизмом должен всё различать (AR-80).
5. **Данные пересекают границу модуля только контрактом.** Прямой SQL в схему
   контингента или календаря запрещён (AR-45, AR-68).
6. **Литеральный hex в компоненте — дефект.** Только токены, иначе проверка
   контраста G-37 ничего не гарантирует.
7. **Недоступное роли не рендерится.** Пустое состояние не показывает кнопку,
   которой у роли нет (AR-69).
8. **Не додумывать.** Нет в спеке — значит нет; поведение не описано — это
   дефект спеки, о котором надо сообщить, а не заполнить собой.
9. **История не переписывается пересборкой.** Отметка, поставленная человеком,
   переживает любую регенерацию расписания: урок отвязывается, а не удаляется
   (AR-85). Удаление отметки — только явное действие педагога.

## 6. Чего в 1.1.1 нет

КТП и КПП · смена номера телефона · деактивация сотрудников · кабинеты детей и
родителей (1.1.3) · Документохранилище (1.1.2) · Communitoria и любые её экраны
(AR-59) · тёмная тема · триместры · импорт контингента файлом · ручная правка
сетки · замены и переносы уроков · поиск, фильтры, сортировка · экспорт и печать ·
уведомления · планшетная раскладка · выдвижной бургер-сайдбар на мобайле.

Если элемент кажется необходимым — это вопрос владельцу, а не инициатива агента.

## 7. Как сверяться

```bash
npm run method:lint      # реестры, линзы, спека, контраст, экраны, адаптация
node specs/school-onboarding/model/walk.mjs    # FSM: тупики, недостижимость
node specs/school-onboarding/model/props.mjs   # генератор, журнал, контингент
node specs/school-onboarding/model/sim.mjs     # нагрузка на модератора
```

Утверждение «готово» принимается только со ссылкой на зелёную проверку. Раздел
инвентаря §3 — чек-лист: каждая незакрытая строка означает, что версия не
собрана, даже если экраны выглядят законченными.
