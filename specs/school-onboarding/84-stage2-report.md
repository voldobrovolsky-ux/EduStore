# Отчёт этапа 2 — десктопный интерфейс (2026-08-21)

> Этап 2 по [80-code-prompt.md](./80-code-prompt.md): все 23 экрана в раскладке
> `desktop` (≥768px) по реестру [70-screens.md](./70-screens.md), оболочка
> AR-81, пятнадцать модалок AR-82, девять типов кнопок, три обязательных
> состояния каждого экрана, дизайн-токены без единого литерального цвета.
> Мобильная раскладка — этап 3, здесь её нет и она честно не заявлена.
>
> Правило отчёта прежнее: «работает» принимается только со ссылкой на зелёную
> проверку. Ниже у каждого пункта либо ворота, либо честное «не сделано».

---

## 0. Короткий итог

| | |
|---|---|
| Экранов реализовано | 23 из 23 (десктоп) |
| Элементов реестра поимённо | **151 из 151**, ворота G-52 |
| Шагов браузерного смока | 38 со скриншотами, ворота G-53 |
| Новых ворот | G-52, G-53; расширены G-37 и G-41 |
| Новых АР-решений | AR-113, AR-114 (находки), AR-115, AR-116 |
| Ворот этапа 1 | 16 из 16 зелёные после всех правок, 409 утверждений |
| Дефектов найдено смоком и исправлено | 4 (см. §5) |

Главный результат: **пустая школа доводится до подтверждённой сетки и живого
журнала в браузере, без единого обращения к API руками** — кроме двух шагов,
у которых десктопного пути нет по конструкции (§4.3).

---

## 1. Что сделано

### 1.1 Каркас контура

`apps/web/src/schoolium/` — отдельный контур рядом с вытесняемым кабинетом
КТП/КПП (AR-83, AR-84), ни одного общего компонента:

| Модуль | Назначение |
|---|---|
| `tokens.css` | 74 CSS-переменные, **сгенерированы** из `docs/design/tokens.json` |
| `app.css` | десктопная таблица стилей: оболочка, кнопки, карточки, модалки, состояния, таблицы, журнал, сетка недели, контур входа |
| `api.ts` | единый HTTP-слой; формы — из `@edustore/shared`, дрейф ломает `tsc` (AR-36) |
| `session.tsx` | `GET /api/v1/me` → роли и права; клиент их **не вычисляет** (AR-7, AR-35) |
| `router.tsx` | маршруты по пути (AR-41), карта сайта из `30-spec.md` |
| `hooks.ts` | `useAsync` (три состояния экрана), `usePolling` (2 с, AR-87) |
| `ui.tsx` | девять типов кнопок, поле, модалка, **поповер**, скелетоны, пустое/ошибочное состояние, тост, аватар, бейдж, чип отметки |
| `shell.tsx` | сайдбар 240/72 + топбар 64 (AR-81), меню пользователя `M-15` |
| `SchooliumApp.tsx` | карта маршрутов, гейт сессии, 403 для `/admin` |
| `screens/*.tsx` | шесть модулей по разделам: `auth`, `classes`, `subjects`, `staff`, `schedule`, `journal`, `misc` |

Правила, зашитые в библиотеку, а не оставленные на дисциплину экрана:
кнопка, недоступная роли, **не рендерится** (AR-69); модалка держит фокус
внутри, закрывается `Esc`, крестиком и кликом по фону, возвращает фокус
открывателю; пустое состояние показывает кнопку только при наличии права;
ни одного литерального цвета — всё через переменные.

### 1.2 Двадцать три экрана

- **Контур входа без оболочки:** `S-00` лендинг (кнопки регистрации нет и не
  будет, AR-95), `S-01` QR привязки устройства с самоперевыпуском просроченного
  кода, `S-05` шесть цифр кода модератора, `S-03` регистрация по QR, `S-04`
  фото (пропуск — полноценный путь), `/bootstrap/:token` (AR-93).
- **Контингент:** `S-10` сетка классов, `S-11`/`M-01` мастер из пяти шагов в
  порядке реестра с кнопками-отказами «⌀ Без литер» и «⌀ Без групп» (AR-77) и
  превью, перечисляющим **имена** классов (Д5); `S-12` журнал контингента со
  строками-приглашениями; `S-13`/`M-02` профиль ученика; `M-13` подтверждение,
  называющее заполненные профили отдельно от пустых (AR-105).
- **Предметы:** `S-20` карточки на пару «предмет × класс», `M-03` создание,
  `S-21`/`M-04` карточка с покрытием, `S-22`/`M-05` QR привязки — второй и
  последний уровень вложенности (AR-82).
- **Персонал:** `S-30` три секции по уровням, кнопка «Добавить» только у
  множественных ролей; `S-31`/`M-06` карточка с QR активации, кодом входа,
  ролями, `M-07` и `M-13`.
- **Расписание:** `S-40` сетка недели с плашкой «устарело»; `S-41`/`M-08`
  мастер из четырёх экранов (четверти → нагрузка → приоритеты → параметры дня)
  с единственной розовой кнопкой «Сгенерировать» (AR-80); `S-42` прогресс,
  отказ с переходом на тот экран мастера, где собирается недостающий вход
  (AR-111), предпросмотр и подтверждение (AR-18: без нажатия ничего не
  материализуется).
- **Журнал:** `S-50` таблица с липкой колонкой ФИО и липкой шапкой дат,
  серыми колонками будущих уроков и колонками «вне расписания», стрелками по
  ячейкам и `Enter` на выбор отметки; `S-51` поповер темы; `S-52` шесть чипов
  в фиксированном порядке `5 4 3 2 н б` (AR-79) и «Убрать отметку».
- **Прочее:** `S-60` кабинет модератора с журналом собственных действий;
  `S-70` сканер — на десктопе причина, а не заглушка; `S-80` устройства и
  сессии; 403-экран для `/admin`.

### 1.3 Граница двух контуров

Правило разделения одно и живёт в одном месте (`apps/web/src/main.tsx`):

1. `?pilot=`, `?bind=`, `?screen=` — маршруты вытесняемого контура (его смок
   G-9 ими пользуется);
2. пути Schoolium — контуру 1.1.1;
3. корень `/` — контуру 1.1.1 (AR-95), **кроме** браузера с живой legacy-сессией.

Различать контуры научился `GET /api/v1/me`: он теперь отвечает только сессии
версии 1.1.1 (её признак — непустой массив ролей, который заполняет контур
доступа; у OIDC-сессий он пуст по построению). Без этого различения владелец
legacy-кабинета попадал бы на лендинг вместо своего кабинета.

---

## 2. Решения по находкам (АР)

Четыре решения этапа. Каждое: находка → почему нельзя оставить → что принято.

### AR-113 · У отказа «удаление записи с историей» есть имя — два

Правило подмены кнопки «удалить» → «деактивировать» решает сервер (AR-78,
AR-89), но гейт живёт в контракте (красная линия 3), и случай, когда интерфейс
показывает «Удалить», а сервер обязан отказать, не теоретический: модератор
открыл карточку при `hasMarks: false`, педагог в этот момент поставил отметку.
Подменять отказ кодом `CLASS_HAS_MARKS` нельзя — тот про класс. Введены
`STUDENT_HAS_MARKS` и `STAFF_HAS_HISTORY`; кодов версии стало **29**.
Ворота: G-43, G-48.

### AR-114 · Потолок уроков в день — у каждой параллели свой

СанПиН даёт разные потолки первому и восьмому классу (4 и 7). Один потолок на
школу либо запрещает старшим классам законные уроки, либо разрешает младшим
незаконные. Принято: `slotsPerDay` — верхняя граница школы, а каждый класс
получает `min(slotsPerDay, потолок своей параллели)`; недельная сетка считается
поклассно. Поле в `S-41` несёт подсказку об этом дословно.
Ворота: G-46, G-34.

### AR-115 · Средний балл — колонка журнала, а не невидимое поле контракта

`JournalDto.average` появился на этапе 1, потому что `30-spec.md` прямо говорит
«средний балл считается чтением», — но экранный реестр не содержал элемента,
который бы его показывал. Контракт возил на клиент число, которому некуда
попасть, а правило «не выдумывать» запрещало нарисовать колонку по своей воле.
Принято: в `S-50` внесён `S-50.col.average` — последняя колонка, только чтение,
без права и без действия. Правило расчёта не изменилось: среднее по числовым
2–5, «н» и «б» не участвуют, числовых нет — «—», а не ноль.
Ворота: G-38, G-52; свойство P7 расширено тремя утверждениями.

### AR-116 · Строка аудита называет объект тем, что аудит о нём знает

`S-60.audit` требует «дата · действие · объект», а леджер по AR-30 хранит
конверт события, но не доменную нагрузку: имени «5А» в нём нет и по построению
быть не должно. Расширять `AuditLog` доменной подписью — значит заставить аудит
знать правила именования каждого контура. Принято: **действие** — подпись из
карты `AUDIT_LABELS`, полнота которой доказывается перечислением по контракту
событий; **объект** — ФИО субъекта ПДн, если событие о человеке, иначе тип
объекта из имени события. Кабинет спрашивает аудит читающим контрактом
`listByActor`, а не запросом в его таблицу.
Ворота: G-41 (три новых утверждения).

---

## 3. Ворота

### Новые

| G | Команда | Что доказано |
|---|---|---|
| **G-52** | `node tools/method/check-testids.mjs` | Каждый из **151** идентификатора реестра стоит в модуле своего экрана; ни один модуль не носит элементов чужого экрана. Одиннадцать идентификаторов закрыты шаблоном (`S-52.chip.${…}`) — **каждый такой случай печатается**, буквальное присутствие доказывает G-53 |
| **G-53** | `npm --workspace apps/web run smoke:onboarding` | Chromium ведёт пустую школу от bootstrap-ссылки до подтверждённой сетки и журнала: **38 шагов со скриншотами**, на каждом экране проверяются его идентификаторы |

### Расширенные

- **G-37** дополнен второй половиной: литеральных цветов в компонентах контура
  **ноль** (проверено перечислением по 15 файлам), и `tokens.css` сверен с
  `tokens.json` (`tokens-to-css.mjs --check`, 74 переменные). Без этого первая
  половина ничего не значит: цвет, вписанный в компонент мимо токенов, не
  участвует ни в одной паре контраста.
- **G-41** дополнен тремя утверждениями AR-116: у каждого из 22 событий есть
  подпись действия, у каждой подписи назван тип объекта, подписей ровно столько,
  сколько событий (карта не переживает удаление события).

### Зелёные (прогон этапа 2)

- `npm run method:lint` — G-19, G-20, G-21, G-37, G-38, G-39, G-40, G-52 ✅
- `npm --workspace apps/api run moderator:check` — G-41, 31 утверждение ✅
- `npm --workspace apps/web run smoke:onboarding` — G-53, 38 шагов, 0 нарушений ✅
- `node e2e/smoke-textbook.mjs` — G-9 вытесняемого контура, 9 скриншотов ✅
  (прогон на чистой базе: именно он и обнаружил дефект №1 в §5)
- `npx tsc -p apps/api/tsconfig.json --noEmit` и `apps/web` — чисто ✅
- Сборка API и веба — чисто ✅

---

## 4. Что НЕ сделано и почему

### 4.1 Мобильная раскладка

Этап 3. Ни один экран не проверялся на <768px, и заявлять обратное нельзя.
Элементы, которые по `75-adaptive.md` существуют только на мобайле
(`S-70.viewfinder`, `S-70.result`, `S-70.error.denied`), в коде **есть** и
рендерятся по `matchMedia`, но живого прогона на телефоне не было.

### 4.2 Элементы вне пути онбординга

Смок проходит счастливый путь пустой школы, и три элемента на нём не
встречаются: `S-31.badge.inactive` (нужна деактивированная карточка),
`S-40.banner.stale` с кнопкой регенерации (нужна правка после подтверждения),
`S-50.col.future`/`S-50.col.detached`/`S-50.row.inactive` (нужны будущие,
отвязанные уроки и деактивированный ученик). Все они доказаны статически
воротами G-52, а их серверная половина — воротами G-42 и G-35. Смок печатает
об этом строку, а не молчит.

### 4.3 Два шага, которые смок делает контрактом

Регистрация педагога по QR и скан QR привязки — **телефонные** действия:
десктопного пути у них нет по конструкции (`S-70` на десктопе так и написано).
Смок выполняет их тем же контрактом, что и телефон, из отдельного браузерного
контекста. Это стендовое устройство, названное в коде смока прямо, а не
поведение продукта.

### 4.4 Аватар — ссылка, а не файл

`S-04` и `M-15` принимают URL картинки. Файловое хранилище принадлежит контуру
Документохранилища (1.1.2), тянуть его в 1.1.1 рано.

---

## 5. Дефекты, найденные и исправленные по ходу

| # | Дефект | Как нашёлся | Исправление |
|---|---|---|---|
| 1 | **Собранный API не запускался**: `@edustore/shared` не имел сборки, и `require` из `dist/main.js` падал на ESM-резолве. Этап 1 добавил в API импорты **значений** из пакета — до него хватало типов, стиравшихся при компиляции. Это ломало живой смок G-9 | попытка поднять API для G-53 | пакету контрактов дана настоящая сборка (`tsc` → CJS + `.d.ts`); `apps/api` строит его перед собой и перед `schoolium:check`; веб как резолвил исходник алиасом, так и резолвит |
| 2 | **Пустое тело ответа читалось как `{}`**: `GET /schedule` до первой генерации отдаёт пустое тело, клиент делал из него объект, и `S-40` рисовал сетку из `undefined.map` | смок, шаг `S-40` | пустое тело = отсутствие ответа (`null`), а не пустой объект |
| 3 | **Карточка предмета без педагога молчала**: при непокрытом «всём классе» сервер отдаёт `coverageComplete: false` и пустой перечень групп (номер `0` — не группа), и оба ветвления UI давали пусто | смок, шаг `S-20` | третья ветка: «весь класс — нет педагога»; та же правка в `S-21.status.coverage` |
| 4 | **Радиокнопки пола без `value`** — форма, из которой не прочитать выбор | смок, шаг `S-13` | добавлены `value="m"` / `value="f"` |

Плюс два уточнения спеки (правки без АР, по образцу переименования
`subject.deleted.v1` этапа 1):

- `S-70.btn.scanner` и `L.topbar.scan` оказались **двумя именами одной кнопки**
  шапки. Реестр экранов объявлен единственным источником для генерации UI, но
  сама кнопка принадлежит оболочке, и её имя ведёт `75-adaptive.md`. Второе имя
  убрано из `70-screens.md`, вместо него — ссылка на элемент оболочки и новая
  строка `S-70.hint.desktop` для десктопной карточки «Сканер доступен на
  телефоне», которую `75-adaptive.md` требовал, а реестр не называл.
- `S-41.panel.term[1..4]` в реестре означает четыре панели; в коде им
  соответствуют `S-41.panel.term1…4`, и G-52 разворачивает диапазон, а не
  ищет элемент с квадратными скобками в имени.

---

## 6. Как проверить

```bash
npm run method:lint                                   # G-19…G-21, G-37…G-40, G-52
npm --workspace apps/api run schoolium:check          # 16 ворот этапа 1
npm --workspace apps/web run smoke:onboarding         # G-53, скриншоты в e2e/screenshots-onboarding/
```

Смоку нужен живой Postgres; базу он пересоздаёт сам. `CHROMIUM_PATH` — если
браузер Playwright лежит не там, где его ждёт установленная версия.

---

## 7. Что дальше (этап 3)

Мобильная раскладка всех 23 экранов: таб-бар, нижние листы вместо модалок,
полноэкранные потоки мастеров, камера во весь экран на `S-70`, журнал с
горизонтальной прокруткой и липкой колонкой ФИО. Плюс прогон `S-70` на живом
устройстве — единственный экран версии, который нельзя доказать смоком на
десктопе.
