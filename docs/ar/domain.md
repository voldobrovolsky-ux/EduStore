<!-- ar-register: id=domain; ranges=38-39,67-68,72-75,79,84-85 -->

# АР-реестр · Учебный процесс и движок

> Блок ведения: **Учебный процесс и движок**. Владелец: предметная область. Диапазоны номеров: `38-39,67-68,72-75,79,84-85`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-38 | **КТП-авторинг закрыт автогенерацией** (PR #10): `textbook.parsed → ktp.generated` — движок создаёт/дополняет **draft**-КТП (утверждённая версия не трогается, `fgosHours` с бейджем `hoursSource='estimated'`). **Timetable-авторинг (решение владельца 2026-07-28): ручная типовая неделя в кабинете завуча** — экран «Сетка расписания» (класс × день × позиция), CRUD через API движка (движок — единственный писатель, право `schedule.build`). CP-SAT-авторасписание встанет позже за тем же контрактом (Solver заполняет ту же сетку, экран не меняется). Ворота — G-15. | [заменено AR-84] | G-15 | L-2, L-1 |
| AR-39 | **`doc.enrich` извлекает текст сам** (PR #10): PDF — `pdf-parse`, `text/*` — как есть; скан без текстового слоя → `textExtract=null` (деградация). Разбор — за абстракцией `ParserProvider`: дефолт `regexp` (0 ИИ), опционально `LlmParserProvider` (OpenAI-совместимый эндпоинт, ключ AES-256-GCM, per-workspace настройка в админке) с fallback в regexp без падения загрузки. Тяжёлый Vision-OCR для сканов — остаётся слотом. | [РЕШЕНО; OCR сканов — слот] | G-5 | L-12, L-6 |
| AR-67 | **Порядок реализации внутри v1 обратен порядку ценности**: календарь учебного года → расписание → журнал. Журнал не существует без сетки, сетка не существует без учебных периодов; начав с журнала, получаем даты-заглушки и переделку. Порядок предъявления пользователю остаётся прежним (журнал — главный экран). | [РЕШЕНО] | нет: план реализации (проверяется на С0 каждой задачи) | L-10, L-1 |
| AR-68 | **Календарь учебного года — сервис-владелец** учебных периодов, каникул, праздников, переносов и дней-исключений (актированные дни, карантин). Расписание и журнал читают его через контракт, как и контингент (AR-45), и **не хранят собственных дат периодов**. Иначе итоговые оценки и сетка разъедутся при первом переносе рабочей субботы. | [РЕШЕНО] | G-31 | L-1, L-6 |
| AR-72 | **Онбординг школы — направленный мастер, а не замок**: классы → предметы → персонал → привязка → четверти → нагрузка → приоритеты → параметры дня → генерация → подтверждение. Каждый пройденный шаг редактируем позже из своего экрана; правки после генерации помечают расписание устаревшим и предлагают регенерацию (не выполняют её сами — AR-18). | [РЕШЕНО] | G-33 | L-2, L-14 |
| AR-73 | **Расписание = шаблон недели + скользящая материализация**: генератор строит недельный шаблон; движок материализует уроки в даты календаря с горизонтом 3 недели, пропуская праздники и каникулы; шаблон повторяется до конца четверти. «3 недели» — горизонт видимости, не длина расписания. | [РЕШЕНО] | G-34 | L-2, L-1 |
| AR-74 | **Журнал производен от расписания**: колонка = материализованный урок (два урока в дату — две колонки); отметки принимаются только за прошедшие и текущие уроки — гейт в контракте, не только в UI; тема урока вводится в поповере колонки. Правка расписания не удаляет колонки с выставленными отметками (они деактивируются с пометкой). | [РЕШЕНО] | G-35 | L-1, L-2 |
| AR-75 | **Группа — именованное подмножество класса**: мастер задаёт количество групп, состав назначается модератором; дефолт-разбиение — по алфавиту поровну с ручной правкой в карточке ученика. Каждый ученик класса состоит ровно в одной группе (при наличии групп); групповые часы одного предмета ставятся в слот парами либо на край дня — полуокно (одна группа учится, вторая без занятия в середине дня) запрещено. | [РЕШЕНО] | G-34 | L-3, L-16 |
| AR-79 | **Шкала отметок журнала** (решение владельца 2026-08-20): числовые 2–5 (участвуют в среднем) + «н» — отсутствие без уважительной причины + «б» — болен (уважительная); нечисловые в средний балл не входят. Таксономия закрыта строкой поведения по всем измерениям в спеке 1.1.1. | [РЕШЕНО] | G-35 | L-3, L-13 |
| AR-84 | **Авторинг сетки 1.1.1 — генератор шаблона недели**, единственный писатель `ScheduleTemplate`/`TemplateSlot`/`Lesson` (право `schedule.build`). Заменяет ручную типовую неделю кабинета завуча (AR-38): экран ручной сетки и его ворота G-15 принадлежат контуру КТП-движка, который в 1.1.1 не входит и выводится отдельным инкрементом. Два писателя одной сетки одновременно не работают: контуры не пересекаются ни в UI, ни в данных 1.1.1. | [РЕШЕНО] | G-34 | L-1, L-2 |
| AR-85 | **Жизненный цикл сетки после подтверждения**: правка контингента (приём, правка профиля, деактивация, перевод между группами) сетку не трогает и оставляет школу в `ready`; правка структуры, привязок, нагрузки, приоритетов, параметров дня и дат четвертей помечает сетку `stale`. При регенерации урок с отметками **отвязывается** (остаётся колонкой журнала с пометкой «вне расписания», новые отметки не принимает, издаётся `schedule.lesson.detached.v1`), урок без отметок исчезает. Отметка не удаляется пересборкой расписания никогда. | [РЕШЕНО] | G-40 | L-2, L-9 |

_Последняя ревизия блока: 2026-08-21 (этап 0: авторинг сетки и её жизненный цикл)._
