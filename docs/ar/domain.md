<!-- ar-register: id=domain; ranges=38-39,67-68,72-75,79,84-85,100-101,103,107,111,114,118-132 -->

# АР-реестр · Учебный процесс и движок

> Блок ведения: **Учебный процесс и движок**. Владелец: предметная область. Диапазоны номеров: `38-39,67-68,72-75,79,84-85,100-101,103,107,111,114,118-132`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-38 | **КТП-авторинг закрыт автогенерацией** (PR #10): `textbook.parsed → ktp.generated` — движок создаёт/дополняет **draft**-КТП (утверждённая версия не трогается, `fgosHours` с бейджем `hoursSource='estimated'`). **Timetable-авторинг (решение владельца 2026-07-28): ручная типовая неделя в кабинете завуча** — экран «Сетка расписания» (класс × день × позиция), CRUD через API движка (движок — единственный писатель, право `schedule.build`). CP-SAT-авторасписание встанет позже за тем же контрактом (Solver заполняет ту же сетку, экран не меняется). Ворота — G-15. | [заменено AR-84] | G-15 | L-2, L-1 |
| AR-39 | **`doc.enrich` извлекает текст сам** (PR #10): PDF — `pdf-parse`, `text/*` — как есть; скан без текстового слоя → `textExtract=null` (деградация). Разбор — за абстракцией `ParserProvider`: дефолт `regexp` (0 ИИ), опционально `LlmParserProvider` (OpenAI-совместимый эндпоинт, ключ AES-256-GCM, per-workspace настройка в админке) с fallback в regexp без падения загрузки. Тяжёлый Vision-OCR для сканов — остаётся слотом. | [РЕШЕНО; OCR сканов — слот] | G-5 | L-12, L-6 |
| AR-67 | **Порядок реализации внутри v1 обратен порядку ценности**: календарь учебного года → расписание → журнал. Журнал не существует без сетки, сетка не существует без учебных периодов; начав с журнала, получаем даты-заглушки и переделку. Порядок предъявления пользователю остаётся прежним (журнал — главный экран). | [РЕШЕНО] | нет: план реализации (проверяется на С0 каждой задачи) | L-10, L-1 |
| AR-68 | **Календарь учебного года — сервис-владелец** учебных периодов, каникул, праздников, переносов и дней-исключений (актированные дни, карантин). Расписание и журнал читают его через контракт, как и контингент (AR-45), и **не хранят собственных дат периодов**. Иначе итоговые оценки и сетка разъедутся при первом переносе рабочей субботы. | [РЕШЕНО] | G-31 | L-1, L-6 |
| AR-72 | **Онбординг школы — направленный мастер, а не замок**: классы → предметы → персонал → привязка → четверти → нагрузка → приоритеты → параметры дня → генерация → подтверждение. Каждый пройденный шаг редактируем позже из своего экрана; правки после генерации помечают расписание устаревшим и предлагают регенерацию (не выполняют её сами — AR-18). | [РЕШЕНО] | G-33 | L-2, L-14 |
| AR-73 | **Расписание = шаблон недели + скользящая материализация**: генератор строит недельный шаблон; движок материализует уроки в даты календаря с горизонтом 3 недели, пропуская праздники и каникулы; шаблон повторяется до конца четверти. «3 недели» — горизонт видимости, не длина расписания. | [РЕШЕНО] | G-34 | L-2, L-1 |
| AR-74 | **Журнал производен от расписания**: колонка = материализованный урок (два урока в дату — две колонки); отметки принимаются только за прошедшие и текущие уроки — гейт в контракте, не только в UI; тема урока вводится в поповере колонки. Правка расписания не удаляет колонки с выставленными отметками (они деактивируются с пометкой). | [РЕШЕНО] | G-35 | L-1, L-2 |
| AR-75 | **Группа — именованное подмножество класса**: мастер задаёт количество групп, состав назначается модератором; дефолт-разбиение — по алфавиту поровну с ручной правкой в карточке ученика. Каждый ученик класса состоит ровно в одной группе (при наличии групп); групповые часы одного предмета ставятся в слот парами либо на край дня — полуокно (одна группа учится, вторая без занятия в середине дня) запрещено. | [РЕШЕНО] | G-34 | L-3, L-16 |
| AR-79 | **Шкала отметок журнала** (решение владельца 2026-08-20): числовые 2–5 (участвуют в среднем) + «н» — отсутствие без уважительной причины + «б» — болен (уважительная); нечисловые в средний балл не входят. Таксономия закрыта строкой поведения по всем измерениям в спеке 1.1.1. | [РЕШЕНО] | G-35 | L-3, L-13 |
| AR-84 | **Авторинг сетки 1.1.1 — генератор шаблона недели**, единственный писатель `ScheduleTemplate`/`TemplateSlot`/`Lesson` (право `schedule.build`). Заменяет ручную типовую неделю кабинета завуча (AR-38): экран ручной сетки и его ворота G-15 принадлежат контуру КТП-движка, который в 1.1.1 не входит и выводится отдельным инкрементом. Два писателя одной сетки одновременно не работают: контуры не пересекаются ни в UI, ни в данных 1.1.1. | [РЕШЕНО] | G-34 | L-1, L-2 |
| AR-85 | **Жизненный цикл сетки после подтверждения**: правка контингента (приём, правка профиля, деактивация, перевод между группами) сетку не трогает и оставляет школу в `ready`; правка структуры, привязок, нагрузки, приоритетов, параметров дня и дат четвертей помечает сетку `stale`. При регенерации урок с отметками **отвязывается** (остаётся колонкой журнала с пометкой «вне расписания», новые отметки не принимает, издаётся `schedule.lesson.detached.v1`), урок без отметок исчезает. Отметка не удаляется пересборкой расписания никогда. | [РЕШЕНО] | G-42 | L-2, L-9 |
| AR-100 | **Нерабочие дни — справочник, версионируемый в коде по учебным годам** (уточнение AR-68): производственный календарь РФ (базис #8) лежит файлом на каждый год, загружается сидом в календарь и правится модератором только в будущих версиях (дни-исключения — слот). **Отсутствие данных на год — именованный отказ `CALENDAR_YEAR_MISSING` при генерации, а не тихий пропуск**: молчание здесь означало бы уроки 23 февраля, обнаруженные школой, а не разработчиком. Годовой файл обновляется одним коммитом при выходе постановления о переносах. | [РЕШЕНО; дни-исключения — слот] | G-45 | L-13, L-12 |
| AR-101 | **Материализация — идемпотентная операция «дозаполнить горизонт», а не разовое событие**: повторный прогон на тех же данных не создаёт ни одного дубля (ключ — дата плюс номер слота). Именно идемпотентность даёт право на три триггера сразу: подтверждение сетки, ночной крон и открытие журнала с горизонтом короче трёх недель. Без неё пришлось бы выбирать один триггер и объяснять школе, почему колонки кончились на четвёртой неделе. | [РЕШЕНО] | G-45 | L-4, L-2 |
| AR-103 | **Дневная сетка задаётся числом уроков в день, а длина дня — производная и проверяемая.** До этого решения «уроков в день» не собиралось ни одним экраном, хотя два арифметических отказа считаются как `дни × слоты` (`LOAD_EXCEEDS_GRID`, `TEACHER_OVERBOOKED`), а ограничение 7 генератора ссылалось на базис #3, который нормирует **недельную**, а не дневную нагрузку. Вводится: поле «уроков в день» на экране 4 модалки расписания; дневной потолок по параллелям из СанПиН 1.2.3685-21 табл. 6.6 (базис #11: 1 кл — 4, 2–4 — 5, 5–6 — 6, 7–11 — 7) с отказом `DAY_EXCEEDS_SANPIN`; проверка длины дня арифметикой `слоты × урок + перемены + большая перемена` с отказом `DAY_TOO_LONG`. Потолок длины дня — **420 минут [дефолт] владельца**, а не норма: СанПиН нормирует урок, перемену и число уроков, но не суммарную длину дня, и подменять продуктовое ограничение ссылкой на закон запрещено (базис, раздел «Не найдено»). Побочный эффект решения — четыре временных параметра экрана 4 (`lessonMin`, `breakMin`, `bigBreakAfter`, `bigBreakMin`) перестают быть мёртвым вводом: до него они собирались, валидировались поодиночке и не влияли ни на один выход. | [РЕШЕНО; потолок 420 мин — дефолт] | G-46 | L-2, L-13 |
| AR-107 | **У перебора есть названный бюджет, и его исчерпание — честный `NO_SOLUTION`.** Требование было названо стендом (`40-bench.md`, «что стенд не ловит» #3: сходимость на 32 классах с литерами не доказана) и промптом кодера, но не числом и ни одними воротами — то есть исполнитель обязан был выдумать величину. Бюджет: **20 секунд либо 200 000 попыток размещения, что раньше** — `[дефолт]`, меняется решением владельца. Исчерпание бюджета отвечает тем же `NO_SOLUTION` с тем же маршрутом восстановления: отдельный код завёл бы человека в тупик «попробуйте ещё раз», которого у него нет. Мастер видит прогресс и кнопку «Отменить» (`S-42.btn.cancelGen`), школа остаётся в `day_params_set`. Зерно перебора пишется в аудит вместе с длительностью — жалоба «сетка не собирается» воспроизводится через `GEN_SEED` (AR-97). | [РЕШЕНО; величины — дефолт] | G-46 | L-12, L-2 |

| AR-111 | **Отказы, считающиеся от «слотов недели», принадлежат экрану 4, а не экрану 2 — потому что второго множителя на экране 2 ещё не существует.** `70-screens.md` относил к экрану 2 (нагрузка) все четыре отказа: `LOAD_EXCEEDS_SANPIN`, `GROUP_HOURS_UNEQUAL`, `LOAD_EXCEEDS_GRID`, `TEACHER_OVERBOOKED`. Но последние два считаются как `дни × уроков в день`, а «уроков в день» собирается ЭКРАНОМ 4 (AR-103) — то есть ПОЗЖЕ нагрузки в порядке мастера. Прямая реализация даёт `slotsPerDay = 0`, и экран 2 отказывает любой ненулевой нагрузке: первый же модератор упирается в «8 часов при 0 слотах недели» и не может пройти мастер вовсе. Решение: экран 2 считает то, что от параметров дня не зависит (`LOAD_EXCEEDS_SANPIN` — недельный потолок СанПиН, `GROUP_HOURS_UNEQUAL` — равенство часов групп), а два грид-отказа проверяются на экране 4 и перед перебором; при ВОЗВРАТЕ на экран 2 после того, как параметры дня уже заданы, они снова считаются там же — данные для них есть. Правило общее: отказ живёт на том экране, где собран последний нужный ему вход, и ни экраном раньше. | [РЕШЕНО] | G-46, G-33 | L-2, L-14 |

| AR-114 | **«Уроков в день» — верхняя граница школьного дня, а дневной потолок СанПиН применяется к КАЖДОЙ параллели отдельно.** AR-103 завёл одно число на школу и сверял его с потолком параллели — но потолок в СанПиН 1.2.3685-21 табл. 6.6 нормирует именно параллель: 1 класс — 4 урока, 7–11 — 7. При одном числе на школу побеждает потолок младшей параллели: школа, где есть и первый класс, и восьмой, обязана поставить 4 урока в день ВСЕМ, то есть 20 слотов недели против 33 часов недельной нагрузки восьмого класса — `LOAD_EXCEEDS_GRID` становится неустранимым, и полная школа не собирается в принципе. Правило: число с экрана 4 — верхняя граница; `DAY_EXCEEDS_SANPIN` срабатывает, только когда оно выше потолка САМОЙ СТАРШЕЙ параллели школы; генератор строит день каждого класса по `min(уроков в день, потолок его параллели)`, и «слоты недели» в `LOAD_EXCEEDS_GRID` считаются поклассно от этого числа. `TEACHER_OVERBOOKED` по-прежнему считается от школьного числа: педагог работает в любой параллели. `DAY_TOO_LONG` — тоже от школьного, это самый длинный возможный день. Уточняет AR-103, не заменяя его: источник числа и потребители четырёх временных параметров остаются прежними. | [РЕШЕНО] | G-46, G-34 | L-2, L-13 |

| AR-118 | **Слой генерации формализован как задача с целевой функцией, а не как поиск первой допустимой сетки.** До этого решения генератор возвращал первую расстановку, прошедшую жёсткие ограничения, и две допустимые сетки были между собой несравнимы: кнопка «регенерировать» оставалась лотереей. Вводится: переменная `x: U → S` (учебный час → слот недели), семь жёстких ограничений H1…H7, восемь арифметических отказов переформулированы как **леммы о пустоте множества допустимых сеток** (принцип Дирихле, потому и считаются до перебора), целевая функция `Π(x) = Σ wᵢ·πᵢ(x)`. Штрафы `πᵢ` и веса `wᵢ` — **целые**: доказательство завершения локального поиска стоит на строгом убывании в `ℤ≥0`, а свёртка на вещественных числах зацикливается на разнице порядка машинного эпсилон, и таймер это прячет, а не исключает. Нормированные `Qᵢ = 1 − πᵢ/πᵢᵐᵃˣ ∈ [0,1]` — представление для человека. | [РЕШЕНО] | G-56 | L-2, L-17 |
| AR-119 | **Восемь маркеров качества с явными верхними границами.** `prio` (8), `teacherGap` (7), `subjectSpread` (6), `dayBalance` (5), `stability` (4), `teacherBalance` (3), `groupEdge` (2), `firstLast` (2). Порядок весов обоснован: правовой приоритет трудных предметов (МР 2.4.0331-23, базис #13) выше цены окна педагога, та выше эстетики сетки; величины — `[дефолт]` владельца, и ссылаться на СанПиН в объяснении веса запрещено — меры качества расписания федеральные нормы не задают вовсе. Верхняя граница каждого штрафа вычисляется явно (`maxSpread` — точный максимум разброса, а не оценка сверху): без неё `Qᵢ ∈ [0,1]` не выводится и «качество 87 %» не имеет области определения. Маркер `stability` без подтверждённой сетки показывается неактивным, а не единицей: единица означала бы «расхождений нет», хотя сравнивать было не с чем. | [РЕШЕНО; веса — дефолт] | G-56 | L-3, L-13 |
| AR-120 | **Автопроверка: нарушение жёсткого инварианта — дефект движка, а не ввода человека.** Инварианты `I-1…I-8` (машинная форма H1…H7 плюс проверка границ) применяются в четырёх точках: после генерации, после каждого хода автокорректировки, до применения ручного хода, до публикации снимка. Сетка с нарушением наружу не выходит ни в каком виде — ответ `INVARIANT_BROKEN` с номером инварианта и адресом ячейки. Показать человеку сетку, про которую известно, что она нарушает норму, — худший из исходов: школа поставит её в работу, а обнаружит это надзор. Полнота набора доказывается не рассуждением, а порчей допустимой сетки: каждая подделка обязана быть поймана своим инвариантом. | [РЕШЕНО] | G-57 | L-13, L-17 |
| AR-121 | **Автокорректировка — локальный поиск с доказанным завершением и названной окрестностью.** Окрестность `N(x)` замкнута: `MOVE(u → день)` в первую свободную позицию класса и `SWAP` двух часов одного класса. Других ходов нет по построению — инвариант `I-5` (без окон) делает занятые позиции дня префиксом, и перебор всех `(день, позиция)` тратился бы на заведомые отказы (первая редакция модели: 12 544 хода на шаг против 1288 после сведения). Принимается только ход, оставляющий сетку допустимой и **строго** уменьшающий `Π`; отсюда завершение: последовательность в `ℤ≥0` строго убывает и обрывается не позже чем через `Π(x₀)` шагов. Бюджет 5 с / 2000 ходов `[дефолт]` — потолок ожидания человека, не гарантия остановки. Поиск детерминирован: два прогона на одном входе дают одну сетку, иначе жалоба «после исправления стало хуже» невоспроизводима. | [РЕШЕНО; бюджет — дефолт] | G-58 | L-4, L-11 |
| AR-122 | **Ручной ход и ход машины — один объект; различие в праве принять ухудшающий ход.** Три исхода перечислены: нарушает жёсткий инвариант — отказ `MOVE_REJECTED` с номером инварианта и адресом, применение невозможно ни при каком подтверждении; не ухудшает `Π` — применяется; ухудшает — `MOVE_DEGRADES` с названием маркера и величиной, повтор с `confirm` применяет ход и ставит на час признак `manual`. Признак обязателен: без него следующий прогон автокорректировки вернул бы час назад, и человек счёл бы, что интерфейс его обманул. Обратимость по AR-90: `MOVE` обращается `MOVE`, `SWAP` самообратен, кнопка «отменить» применяет обратный ход. | [РЕШЕНО] | G-59 | L-9, L-15 |
| AR-123 | **Графическая маркировка — четыре независимых слоя поверх сетки, а не раскраска ячеек**: базовый (предмет, педагог, группа), качество (вклад ячейки в штраф), происхождение (`авто` / `правка` / `рука`), состояние (устарело, отвязано). Цвет не является единственным носителем смысла (базис #17, WCAG 1.4.1): каждый маркер несёт иконку и текст, распечатка в оттенках серого читается без потери информации. Панель качества на `S-40` называет цену — агрегат и восемь строк «маркер, значение, сколько ячеек виновны» с подсветкой по нажатию, — и не советует, что делать. | [РЕШЕНО] | G-56 | L-15, L-16 |
| AR-124 | **Печать, файл и ссылка — проекции ОДНОГО снимка, а не три сборки.** `ScheduleSnapshot` иммутабелен и несёт версию сетки. Причина в арбитре: собранные тремя путями, распечатка, файл и присланная родителю ссылка расходятся, и школа не сможет доказать, что было в расписании в четверг. Персональных данных учеников снимок не содержит по построению — слот занимает класс либо группа, ученик в сетке не фигурирует. | [РЕШЕНО] | G-60 | L-1, L-7 |
| AR-125 | **QR — транспорт ссылки, а не формат данных.** В код кладётся подписанный https-адрес снимка, не сама сетка: сетка в код не помещается и, что важнее, не отзывается — розданный по школе бумажный код с данными внутри живёт вечно. Подпись `HMAC-SHA256` покрывает снимок, область и срок, поэтому проверяется без обращения к БД (базис #16). Область перечислена: `class`, `teacher`, `school`; ссылка области `teacher` не выдаёт сетку других педагогов. Право на выдачу — отдельный код `schedule.share`, а не `schedule.build`: построить сетку и вынести её за пределы школы разные по последствиям действия. | [РЕШЕНО] | G-60 | L-7, L-13 |
| AR-126 | **По одному владельцу на формат выдачи**: печать — веб (`@media print`, A4 альбомная, страница на класс либо педагога); таблица — API (CSV, UTF-8 с BOM, разделитель `;`); календарь — API (ICS по RFC 5545: одно правило `RRULE` недельного повтора на слот и `EXDATE` из нерабочих дней календаря, AR-100). Список уроков вместо правила повтора ломается при первом сдвиге четверти, правило — нет. PDF версия не отрисовывает: диалог печати браузера выдаёт его сам, а серверная отрисовка (шрифты, кириллица, вёрстка) не оплачена ни одним требованием блока — слот. | [РЕШЕНО; PDF и XLSX — слот] | G-60 | L-6, L-12 |
| AR-127 | **Версия сетки входит в подпись ссылки, поэтому новая версия обрывает старую ссылку сама** — без отдельной операции отзыва и без записи в БД при проверке. Выданная ссылка живёт 90 дней `[дефолт]` либо до отзыва человеком; истёкшая и отозванная отвечают **одинаково по форме и времени** — различить снаружи причину нельзя; сетка, изменившаяся после выдачи, отвечает `SHARE_VERSION_STALE`. Ссылка, молча показывающая прошлогоднее расписание, — худший исход, чем ссылка, честно сказавшая «устарела». | [РЕШЕНО; срок — дефолт] | G-60 | L-9, L-12 |
| AR-128 | **Качество называется вместе со шкалой: агрегат без потолка и размен без раскрытия — числа, которые нельзя прочитать.** Вопрос владельца «то есть качество продукта 50 % от эталонного?» показал, что величина `Π` читается как доля, хотя она сумма штрафов, а найденный локальный минимум читается как эталон, хотя эталон не вычислялся. Вводятся две вещи. **Нижняя граница `πᵢᴸᴮ`** — величина, ниже которой не опускается никакая расстановка при данной нагрузке; выводится арифметикой (`2·r·(d−r)` для разбросов, `max(0, h−d)` для сгущения предмета, излишек приоритетных над ёмкостью ранних позиций), для `teacherGap`, `groupEdge` и `stability` равна нулю. Потолок `Q_LB` показывается рядом с агрегатом; граница честно называется границей, а не эталоном — достижимость её не доказана, и настоящий минимум лежит между ней и локальным. **Размен `traded`** — список маркеров, ухудшившихся при падении `Π`: скалярная свёртка обязана разменивать, и это её правильное поведение, но человек, читающий «качество выросло» рядом с приоритетным предметом, ставшим последним уроком вдвое чаще, обманут агрегатом. На первой школе размен есть и измерен: `firstLast` 11 → 20 при `Π` 2276 → 1073. | [РЕШЕНО] | G-56, G-58 | L-17, L-11 |
| AR-129 | **Бюджет генерации выбирает человек, и он измеряется минутами, а не секундами** (решение владельца 2026-08-27, уточняет AR-107). Сегмент «30 секунд · 2 минуты · 5 минут», дефолт 2 минуты. Расписание настраивается раз в четверть, и пять минут ожидания дешевле недели ручной пересборки. Стратегия поиска при таком бюджете меняется: вместо одного прогона движок делает **мультистарт** — замеряет цикл «генерация + локальный спуск», делит бюджет на замер и берёт лучшую сетку. Измерено на первой школе: 20 перезапусков за 73 секунды дают Π = 948 против 1073 у одиночного прогона (минус 28 % оставшегося зазора) тем же кодом. Число перезапусков — вывод, а не ввод: просить у модератора величину, смысла которой он знать не обязан, значит подменять настройку гаданием. Принцип AR-107 («у перебора есть названный бюджет, исчерпание — честный NO_SOLUTION») остаётся; меняются величины и стратегия. | [РЕШЕНО; величины — решение владельца] | G-61 | L-16, L-11 |
| AR-130 | **Генерация становится асинхронной задачей с поллингом.** Пять минут не переживает ни таймаут HTTP-клиента, ни обратный прокси, ни мобильную сеть: синхронный запрос на такой бюджет — не долгий ответ, а гарантированная ошибка у части школ. `POST /api/v1/schedule/generate` возвращает `jobId` немедленно, экран `S-42` опрашивает состояние раз в 2 секунды — тем же поллингом, что ожидание скана QR (AR-87). Прогресс называет работу числом («перезапуск 7 из 20, лучшее качество 88 %»), а не крутит спиннер: пять минут молчания человек читает как зависание и перезагружает страницу. Отмена останавливает задачу, школа остаётся в `day_params_set`. | [РЕШЕНО] | G-61 | L-12, L-14 |
| AR-131 | **Реестр параметров — один источник для экранов и для генератора; норму можно ужесточить, ослабить нельзя.** Список параметров прозой расходится с кодом молча, и версия 1.1.1 наступила на это дважды: четыре временных параметра собирались и не влияли ни на что (AR-103), а «уроков в день» не собирались вовсе, хотя были входом двух отказов. Реестр живёт данными (`packages/shared/src/schedule-parameters.ts`), документ `35-parameters.md` — его человеческая проекция. Три природы величины различаются явно: **ввод** (задаёт модератор), **норма** (закон, константа), **вывод** (считает движок). Правило нормы: модератор вправе поставить урок 40 минут вместо 45 и 5 уроков в день вместо 6, но не 50 и не 8 — ни при каком подтверждении, потому что это факт закона, а не уровень доступа. Каждый параметр несёт область, дефолт внутри неё и **названного потребителя** — ограничение, маркер либо проекцию выдачи; параметр без потребителя есть мёртвый ввод и ворота его не пропускают. | [РЕШЕНО] | G-61 | L-1, L-18 |
| AR-132 | **Каждый новый параметр-ограничение несёт лемму непустоты, считаемую до перебора.** Шаги «Правила предмета» и «Предпочтения педагога» добавляют семь жёстких ограничений H8…H14 (часов предмета в день, сдвоенные уроки, запрет первой и последней позиции, край дня для групп, методический день и недоступные слоты, максимум уроков педагога в день, жёсткое «без окон»). Каждое сужает область допустимых сеток, поэтому каждое обязано отвечать арифметикой: при бюджете в пять минут отказ `NO_SOLUTION` без диагноза стоит человеку пяти минут ожидания и не говорит ему ничего. Заведены восемь кодов: `SUBJECT_MAX_PER_DAY_UNREACHABLE`, `PAIRED_HOURS_ODD`, `PAIRED_FORBIDDEN_IN_PRIMARY`, `SUBJECT_POSITION_IMPOSSIBLE`, `GROUP_EDGE_UNREACHABLE`, `TEACHER_UNAVAILABLE_OVERBOOKED`, `TEACHER_MAX_PER_DAY_UNREACHABLE`, `TEACHER_NO_GAPS_UNREACHABLE`. Следствие названо, а не скрыто: чем больше жёстких требований задано, тем чаще ответом будет `NO_SOLUTION` при верной арифметике, поэтому экран считает заданные жёсткие требования и при отказе называет последнее добавленное первым кандидатом на ослабление. | [РЕШЕНО] | G-61 | L-2, L-13 |

_Последняя ревизия блока: 2026-08-27 (реестр параметров, бюджет 30 с…5 мин и асинхронная генерация — решение владельца; шкала качества и раскрытие размена — вопрос владельца о «50 % от эталона»; слой качества, корректировки и выдачи расписания — блок «Расписание» УТЦ, AR-118…AR-127; поклассный дневной потолок — этап 2; принадлежность отказов экранам мастера — этап 1; дневная сетка и бюджет перебора — генеральный аудит перед кодом)._
