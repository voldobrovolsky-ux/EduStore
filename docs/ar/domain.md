<!-- ar-register: id=domain; ranges=38-39,67-68 -->

# АР-реестр · Учебный процесс и движок

> Блок ведения: **Учебный процесс и движок**. Владелец: предметная область. Диапазоны номеров: `38-39,67-68`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-38 | **КТП-авторинг закрыт автогенерацией** (PR #10): `textbook.parsed → ktp.generated` — движок создаёт/дополняет **draft**-КТП (утверждённая версия не трогается, `fgosHours` с бейджем `hoursSource='estimated'`). **Timetable-авторинг (решение владельца 2026-07-28): ручная типовая неделя в кабинете завуча** — экран «Сетка расписания» (класс × день × позиция), CRUD через API движка (движок — единственный писатель, право `schedule.build`). CP-SAT-авторасписание встанет позже за тем же контрактом (Solver заполняет ту же сетку, экран не меняется). Ворота — G-15. | [РЕШЕНО] | G-15 | L-2, L-1 |
| AR-39 | **`doc.enrich` извлекает текст сам** (PR #10): PDF — `pdf-parse`, `text/*` — как есть; скан без текстового слоя → `textExtract=null` (деградация). Разбор — за абстракцией `ParserProvider`: дефолт `regexp` (0 ИИ), опционально `LlmParserProvider` (OpenAI-совместимый эндпоинт, ключ AES-256-GCM, per-workspace настройка в админке) с fallback в regexp без падения загрузки. Тяжёлый Vision-OCR для сканов — остаётся слотом. | [РЕШЕНО; OCR сканов — слот] | G-5 | L-12, L-6 |
| AR-67 | **Порядок реализации внутри v1 обратен порядку ценности**: календарь учебного года → расписание → журнал. Журнал не существует без сетки, сетка не существует без учебных периодов; начав с журнала, получаем даты-заглушки и переделку. Порядок предъявления пользователю остаётся прежним (журнал — главный экран). | [РЕШЕНО] | нет: план реализации (проверяется на С0 каждой задачи) | L-10, L-1 |
| AR-68 | **Календарь учебного года — сервис-владелец** учебных периодов, каникул, праздников, переносов и дней-исключений (актированные дни, карантин). Расписание и журнал читают его через контракт, как и контингент (AR-45), и **не хранят собственных дат периодов**. Иначе итоговые оценки и сетка разъедутся при первом переносе рабочей субботы. | [РЕШЕНО] | G-31 | L-1, L-6 |

_Последняя ревизия блока: 2026-08-20 (объём v1: роли, кабинеты, инкременты)._
