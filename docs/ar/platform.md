<!-- ar-register: id=platform; ranges=8-15,36-37,49-51,63-65,76,86-87 -->

# АР-реестр · Стек и рантайм

> Блок ведения: **Стек и рантайм**. Владелец: архитектура ядра. Диапазоны номеров: `8-15,36-37,49-51,63-65,76,86-87`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-8 | **Модульный монолит** NestJS + Prisma + PostgreSQL; фронт React+Vite; общие контракты в `packages/shared`. Микросервисы не вводятся; единственный отдельный рантайм — ASR (Python/faster-whisper) за анти-коррупционным фасадом `voice`. (ADR-0001) | [РЕШЕНО] | нет: структурное (ADR-0001) | L-5, L-10 |
| AR-9 | Из v1 вырезаны: K8s, Terraform, ClickHouse, OpenSearch, Module Federation, React Native, Python-ML. Вводятся по сигналу нагрузки (критерии — INFRA.md). | [РЕШЕНО] | нет: критерии в INFRA.md | L-10 |
| AR-10 | Шина событий v1 — **in-process** (`InProcessEventBus`) + outbox/inbox. NATS JetStream — слот (замена `useClass`). **Граница ввода NATS = второй инстанс API**: `OutboxDispatcher` без `FOR UPDATE SKIP LOCKED` не готов к горизонтальному масштабированию — это осознанный ограничитель, а не баг. | [РЕШЕНО] | G-4 | L-4, L-9 |
| AR-11 | PK = **cuid** (не ULID из Техспеки), колонки **camelCase** (не snake_case). Консистентно со всеми таблицами; подтверждено владельцем; обратимо через `@map`. (ENGINE.md) | [РЕШЕНО] | нет: структурное (schema.prisma) | L-1 |
| AR-12 | Хранение файлов — S3-совместимая абстракция `StorageProvider`, **ленивый клиент** (без конфига приложение живёт, файловые операции отдают 503). Загрузка только presigned PUT → commit с HEAD-валидацией. Единственный писатель файлов — `DocModule`. | [РЕШЕНО] | G-7 | L-6, L-12 |
| AR-13 | ASR — faster-whisper **int8 на CPU, без GPU** (T4 ≈ 610k ₽/мес — разорительно); биасинг словарём класса через `initial_prompt`. Деградация мягкая: ASR недоступен → журнал работает вручную. | [РЕШЕНО] | нет: ручной прогон ASR | L-12, L-16 |
| AR-14 | Аутентификация — три жёстко разделённых режима `AUTH_MODE`: `flor` (OIDC BFF), `dev|test|ci` (bypass-заголовки), `pilot-qr` (временный, выключает dev-bypass). Fail-closed при любом ином значении. | [заменено AR-49] | G-6 | L-14, L-6 |
| AR-15 | Пилотная сессия (QR) пишется в ту же `Session` и несёт **ту же форму claims, что Флёр OIDC** → RBAC не требует спецкейсов при миграции. Workspace пилота постоянный (`ws-archimed-pilot`). Сверка пилотных `pilot-<hex>` id ↔ реальных `sub` — отдельный миграционный шаг, скрипт не написан. | [заменено AR-49] | G-6 | L-14 |
| AR-36 | **Контракты фронт↔бэк**: `packages/shared` покрывает ~20% вызовов; edu/doc/structure/pilot типизированы дважды. Дефолт: единый HTTP-слой (`http.ts`), все DTO — в shared; прод-хардкод `x-florus-user-id: "teacher-anna"` удаляется. | [дефолт] | G-14 | L-6, L-18 |
| AR-37 | **REST-префикс**: движок на `/api/v1/edu`, legacy-кабинеты на `/api/<module>`. Дефолт: унификация под `/api/v1` при ребилде кабинетов (план уже в ENGINE.md). | [дефолт] | нет: план в ENGINE.md | L-6, L-10 |
| AR-49 | **Аутентификация v1 — регистрация и вход по номеру телефона (OTP), собственный контур**; OIDC внешнего IdP не используется. Это полноценное решение текущей версии, а не временный костыль под будущую интеграцию. Заменяет режимы `flor` и `pilot-qr` (AR-14, AR-15); режим bypass для `dev/test/ci` сохраняется с тем же правилом fail-closed при неизвестном значении. | [РЕШЕНО] | G-24 | L-14, L-6 |
| AR-50 | **Порт `IdentityProvider`**: доменные модули не знают способа входа; телефонный контур и будущий единый аккаунт EduStore — адаптеры за одним портом. Существующий OIDC-код этим решением не удаляется, а переводится в неиспользуемый адаптер — удаление есть изменение и требует собственного плана вывода. | [РЕШЕНО] | G-26 | L-6, L-9 |
| AR-51 | **Готовность к размещению в EduStore**: Schoolium публикует версионированный каталог собственных контрактов (события контингента, справочники), пригодный для подключения стороннего сервиса. Витрина, каталог, биллинг EduStore, объединение подписок и SSO — **слот**, сейчас не проектируются. | [РЕШЕНО, слот] | слот: интеграционный слой EduStore | L-6, L-9 |
| AR-63 | **QR — штатный механизм подтверждения и активации операций** (вход, смена телефона, привязка устройства, подтверждение приглашения), а не пилотный костыль. **Обязателен fallback-канал** (одноразовый код на телефон): QR требует второго устройства или физического присутствия, и без запасного пути операция становится невыполнимой для сотрудника вне школы. | [РЕШЕНО] | G-29 | L-14, L-12 |
| AR-64 | **Смена номера телефона — операция из двух шагов**: инициирует модератор школы, завершает владелец аккаунта подтверждением (QR или код). Старый номер освобождается только после успешного завершения; незавершённая смена оставляет доступ по старому номеру. История, членства и согласия висят на `User.id`, а не на телефоне — телефон является ключом входа, но не ключом связей. **Подтверждено владельцем 2026-08-21 (вопрос В4): двухшаговость остаётся единственным исключением из полных прав модератора (AR-88).** Модератор инициирует смену, завершает владелец аккаунта: телефон — ключ входа, и операция над чужим ключом в одиночку превращает полномочия в захват. Операция вне объёма 1.1.1; правило действует с той версии, где она появится. | [РЕШЕНО] | G-30 | L-1, L-14 |
| AR-65 | **Пилотных пользователей нет: регистрация проходит сразу.** Режим `pilot-qr` выводится из эксплуатации; QR сохраняется как механизм подтверждения (AR-63), сменив назначение. Ворота G-6 снимаются вместе с предметом. | [РЕШЕНО] | G-24 | L-14, L-9 |
| AR-76 | **Присутственная QR-регистрация сотрудника** (уточнение AR-49): открытая модератором карточка показывает одноразовый QR; сотрудник сканирует, вводит ФИО и телефон — OTP на этом шаге не отправляется, доверие обеспечивает живая сессия модератора и физическое присутствие. Первый самостоятельный вход сотрудника — уже телефон+OTP (AR-49). QR привязан к конкретной карточке, одноразов, живёт до закрытия карточки. | [РЕШЕНО] | G-36 | L-14, L-8 |
| AR-86 | **Параметры контура доступа названы числами, а не словами**: код OTP — 4 цифры, срок жизни 5 минут, 5 попыток на код, блокировка номера 15 минут, повторная отправка не чаще раза в 60 секунд; QR активации сотрудника — 15 минут или до закрытия карточки, QR привязки педагога — 5 минут, оба одноразовы; сессия — 30 дней с продлением при активности. Значения `[дефолт]`: меняются решением владельца, механику не меняют. Контур входа — последнее место, где исполнителю оставляют выбор по умолчанию. | [дефолт] | G-24 | L-14, L-6 |
| AR-87 | **Ожидание действия на втором устройстве — короткий поллинг, не push**: экраны QR-активации и QR-привязки опрашивают статус токена раз в 2 секунды, пока карточка открыта, и читают `waiting`/`scanned`/`used`/`expired`. WebSocket и SSE в 1.1.1 не вводятся: соединение живёт минуты в присутственном сценарии, а новый транспорт стоит рантайма и отладки в каждой школе. | [РЕШЕНО] | G-36 | L-6, L-14 |

_Последняя ревизия блока: 2026-08-21 (этап 0: параметры доступа и ожидание скана)._
