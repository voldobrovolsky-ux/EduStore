# EduStore — реестр G-проверок

> Дата: 2026-07-28. G-проверка = **исполняемое доказательство инварианта**
> (не «проверено глазами»): скрипт поднимает реальный Nest-контекст + БД и
> перечислением доказывает, что инвариант держится. Утверждение «работает»
> принимается только со ссылкой на зелёную G-проверку.
>
> Формат принят по паттерну shintop (репозиторий недоступен из этой сессии —
> структура выведена из скиллов architecture/review; при расхождении с каноном
> shintop поправить шапку, содержание переносимо). Ссылки АР-N —
> блочные реестры [docs/ar/](./ar/INDEX.md).

## Существующие (зелёные на `a4014cb`)

| G | Команда | Инвариант | Покрывает АР |
|---|---|---|---|
| G-1 | `npm --workspace apps/api run tenant:check` (6/6) | Чтение/запись/`count`/`findUnique` не пересекают границу школы; запрос с тенантом-«не тем» → пусто/`P2025`; аутентифицирован-без-тенанта в guarded-операции → отказ | AR-2 |
| G-2 | `npm --workspace apps/api run minor:check` (10/10) | DM родитель↔свой ребёнок разрешён по ребру; чужой взрослый↔минор запрещён; двойная роль послаблений не даёт; минор↔минор запрещён; external в канал с минором отклонён на добавлении (двусторонне) | AR-27, AR-3 |
| G-3 | `npm --workspace apps/api run comm:check` (20/20) | `mode` не угадывается (`400 MODE_REQUIRED`); suggest-mode advisory (не создаёт сообщение); инвариант миноров срабатывает и на REST-пути; keyset-пагинация; правка ⇒ `edited=true` без стирания; объявление → required-set → ack → overdue → reconcile ушедшего | AR-28, AR-18 |
| G-4 | `npm --workspace apps/api run cascade:demo` | Outbox → 3 подписчика (каналы/питание/УМК); повторная доставка идемпотентна (inbox); петля обрывается depth-guard'ом на MAX_CASCADE_DEPTH | AR-5, AR-21, AR-22 |
| G-5 | `npm --workspace apps/api run parser:check` (13/13) | `doc.file.enriched → textbook.parsed`; payload несёт `fileId`, не `s3Key`; идемпотентность; не-учебник → молчит; пустой `textExtract` → деградация без падения; tenant-изоляция новых таблиц | AR-20, AR-12 |
| G-7 | `npm --workspace apps/api run storage:check` | Живой S3: presign PUT → HEAD → download → сверка контента → delete; пустой конфиг → внятный отказ, не падение | AR-12 |
| G-8 | `npm --workspace apps/api run flow:check` (25) | Сквозной поток «учебник → КТП → КПП → содержание уроков»: авто-контекст загрузки из назначения, живой docs/-контур (`STORAGE_MODE=local`), генерация/дополнение/версионирование КТП, оценка часов, раскладка карт по урокам, fallback llm→regexp, шифрование ключа | AR-38, AR-39, AR-20 |
| G-9 | `node e2e/smoke-textbook.mjs` | Живой Chromium-смок: пилотный QR-вход → загрузка настоящего PDF → скриншоты каждого шага | AR-15, AR-38 |
| G-10 | `npm --workspace apps/api run authz:check` | **RBAC-полнота перечислением**: обход всех контроллеров через ModulesContainer; каждая мутация — `@RequirePermission` либо whitelist-строка с причиной (resource/key/identity-gated); коды сверяются с каталогом и пакетами; протухший whitelist = провал (64 gated / 16 whitelist) | AR-35, AR-7 |
| G-11 | внутри `tenant:check` (кейс 6) | **Tenant fail-closed**: аутентифицированный без школы → `{tenantId:null, system:false}` (не system-обход); guarded-запрос без тенанта → отказ | AR-34 |
| G-12 | структурно: миграция `ar4_unified_journal` + `tsc` | **Единый журнал**: модель `Grade` удалена из схемы — любое обращение не компилируется; метрики/отчёты/голос на `JournalCell`; писатель один (`grade.posted`/`grade.removed`) | AR-4 |
| G-13 | `npm --workspace apps/api run consent:check` (7) | **Consent-гейт**: без `predictive_profiling` — исключён из atRisk с явной пометкой, срез ИОМ → `NO_PROFILING_CONSENT`; согласие открывает, отзыв закрывает; сигналы копятся независимо | AR-29 |
| G-15 | `npm --workspace apps/api run timetable:check` (6) | **Timetable-авторинг сквозной**: сетка через API (не seed) → `INSUFFICIENT_SLOTS` → расширение → Solver `scheduled`; `TIMETABLE_IN_USE` защищает сетку под КПП; `BAD_SLOT` | AR-38 |
| G-16 | `npm --workspace apps/api run events:check` | **Канон событий**: запрет `edustore.*`, все литералы реестров — `<домен>.<агрегат>.<глаголПрош>.v<N>`, wildcard-паттерны валидны; + рантайм-валидация в `newEvent()` (40 литералов) | AR-23, AR-24 |
| G-19 | `node tools/method/lint-ar.mjs` | **Протокол реестров решений**: сквозная нумерация без дыр и дублей, номер внутри диапазона своего блока, диапазоны блоков не пересекаются, `next` в индексе верен, у каждого `[РЕШЕНО]` непустые ворота, все ссылки `AR-*`/`G-*`/`L-*` разрешаются | метод (E-6.1, E-6.3, E-12.3) |
| G-20 | `node tools/method/lint-lenses.mjs` | **Протокол линз**: полнота паспортов, сквозная нумерация, active-линза имеет детектор, каждый пункт чек-листа эволюционной архитектуры связан хотя бы с одной линзой, нет линз-сирот | метод (E-12.1, E-12.2) |
| G-21 | `node tools/method/spec-lint.mjs --self-test` | **Линт спеки работоспособен**: на заведомо плохой фикстуре срабатывают все параметры P1…P7 (линтер, который ничего не находит, — худший вид зелёного) | метод (E-3.4, E-12.4) |

**В CI**: G-1…G-5, G-8…G-13, G-15, G-16 — job `flow-smoke`; G-19…G-21 — job `method-lint`
(бегут без БД и без установки зависимостей). Вне CI остаётся только G-7 (нужен живой S3).

## Недостающие (завести вместе с закрытием соответствующих узлов)

| G | Инвариант, который надо доказать | Условие появления | АР |
|---|---|---|---|
| G-14 | **Контрактная сверка фронт↔бэк — остаток**: structure/timetable уже в `packages/shared` (импортируются обеими сторонами, дрейф ломает tsc; хардкод `x-florus-user-id` удалён); остаток — edu/doc/pilot DTO + типизация engine-контроллера тем же паттерном | следующий инкремент AR-36 | AR-36 |
| G-17 | **DLQ-replay**: событие, добитое до `FAILED`, переигрывается командой; повторная обработка идемпотентна (inbox) | вместе с вводом NATS или ранее | AR-22, AR-24 |
| G-22 | **Иерархия тенантов**: `worknet` не является вторым ключом изоляции — школа-сестра той же сети не видна из workspace; агрегация по сети собирается явным перечислением школ, а не ослаблением tenant-guard; владелец сети не обходит изоляцию | вместе с экраном владельца сети | AR-44, AR-53 |
| G-23 | **Контингент — единственный владелец**: перечислением по всем модулям доказано, что ни один не обращается к таблицам контингента напрямую; все чтения идут через сервис или события; контракт событий контингента версионирован | вместе с выделением контингента за контракт | AR-45, AR-52 |
| G-24 | **Телефонный контур**: регистрация и вход по телефону, одноразовость и срок жизни OTP, уникальность телефона в объявленной области (AR-47), вход в две школы одним телефоном даёт одного пользователя и два членства, регистрация не сообщает о существовании записи в другой школе | вместе с реализацией AR-49 | AR-46, AR-47, AR-49, AR-57 |
| G-25 | **Авторство `parenthood`**: писатель один (модуль контингента); неподтверждённое ребро не открывает приватный DM; инвариант миноров (G-2) держится на новом источнике ребра | вместе с реализацией AR-48 | AR-48 |
| G-26 | **Замена способа входа не трогает домен**: доменные модули не импортируют детали авторизации; подмена адаптера за портом `IdentityProvider` не меняет резолв RBAC | вместе с реализацией AR-50 | AR-50 |
| G-27 | **Разделение обязанностей по человеку, а не по роли**: пользователь, несущий одновременно «преподаватель» и «заместитель по учебной работе», не утверждает собственный КТП; перечислением по всем парам «автор ↔ утверждающий» | вместе с ролевой моделью v1 (AR-60) | AR-61, AR-19 |
| G-28 | **Модератор не завершает операции над чужим аккаунтом в одиночку**: смена телефона и деактивация требуют подтверждения владельцем либо директором/учредителем; все действия модератора присутствуют в аудите | вместе с кабинетом управления школой | AR-62 |
| G-29 | **QR-подтверждение и fallback**: одноразовость, срок жизни, привязка к конкретной операции; отсутствие второго устройства не делает операцию невыполнимой (код на телефон даёт тот же результат) | вместе с реализацией AR-63 | AR-63 |
| G-30 | **Смена телефона не рвёт связи**: история, членства и согласия остаются на `User.id`; старый номер освобождается только после подтверждения; прерванная на середине смена не оставляет пользователя без доступа | вместе с реализацией AR-64 | AR-64 |
| G-31 | **Календарь — единственный владелец периодов**: ни расписание, ни журнал не хранят собственных дат периодов; перенос рабочей субботы виден обоим модулям без ручной синхронизации | вместе с календарём учебного года | AR-68 |
| G-32 | **v1 без детских аккаунтов**: перечислением — ни один сценарий не создаёт вход ученику или родителю; consent-гейт выключен, но не удалён, и его включение не меняет контракты выдач | вместе с объёмом v1 | AR-70, AR-29 |
| G-33 | **Онбординг сквозной перечислением**: обход FSM мастера — из каждого шага есть выход вперёд, возврат для правки и путь к терминалу «школа готова»; правка пройденного шага после генерации помечает расписание устаревшим, но не стирает данные | вместе с реализацией 1.1.1 (заготовка — `specs/school-onboarding/model/walk.mjs`) | AR-72, AR-77 |
| G-34 | **Генератор доказуемо честен**: нагрузка каждого педагога и класса выполнена полностью; педагог не стоит в двух местах одного слота; ученик не занят дважды; полуокон групп нет; превышение СанПиН-потолка — именованный отказ до перебора; материализация пропускает праздники | вместе с реализацией генератора (заготовка — `model/props.mjs`) | AR-73, AR-75 |
| G-35 | **Журнал производен**: колонки = материализованные уроки (2 урока/дата → 2 колонки); отметка в будущий урок отклоняется контрактом; правка расписания не теряет выставленные отметки; деактивированный ученик исчезает из новых колонок, история остаётся | вместе с реализацией журнала (заготовка — `model/props.mjs`) | AR-74, AR-78 |
| G-36 | **QR-активация**: QR одноразов и привязан к карточке; закрытие карточки гасит QR; повторный скан — отказ; зарегистрированный сотрудник входит потом только по телефону+OTP; активация под аудитом с идентичностью модератора | вместе с реализацией персонала 1.1.1 | AR-76, AR-62 |

## Снятые

| G | Почему снята |
|---|---|
| G-18 | **OIDC-цикл вживую** — предмет исчез: решения AR-1 и AR-33 вытеснены (AR-46, AR-49, AR-57), внешний IdP в Schoolium не используется. Снята решением, а не «подгонкой»: правило 2 ниже соблюдено. |
| G-6 | **Пилотный контур (invite→QR→вход→назначение)** — предмет исчез: пилотных пользователей нет, регистрация проходит сразу (AR-65). Проверка была зелёной; её содержательная часть (одноразовость токена, привязка операции к QR) переезжает в G-29, а не теряется. |

Строки реестра, ссылающиеся на снятые ворота (AR-1, AR-14, AR-15, AR-33), сохраняют
ссылку: реестр — журнал, а не текущее состояние.

## Правила ведения

1. Новый инвариант в АР-реестре со статусом [РЕШЕНО] обязан либо ссылаться на существующую G-проверку, либо заводить строку в «Недостающих» в том же коммите. Проверяется машинно — G-19.
2. G-проверка падает → мерж запрещён; чинится код или явным решением меняется АР (с правкой реестра), но не «подгоняется» проверка.
3. G-7 (живой S3) остаётся ручным прогоном перед релизом; всё остальное — в CI.
